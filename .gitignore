# === 시크릿 ===
.env
.env.local
.env.*.local

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.uv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# === Node.js ===
node_modules/
.next/
out/
dist/
build/
.turbo/
tsconfig.tsbuildinfo
.pnpm-debug.log*
.npm/

# === 런타임 데이터 ===
data/db/*.sqlite
data/db/*.sqlite-journal
data/db/*.sqlite-wal
data/db/*.sqlite-shm
data/backups/
data/reports/
data/memory/
data/notifications/
data/snapshots/
data/analyst_queries/
data/strategist_queries/

# === 시드 데이터는 커밋 ===
!data/seed/

# === 학습부별 벡터 인덱스 (Chroma) ===
data/chroma/
# legacy team 벡터 인덱스 (제거 대상, INFRA-RAG-001 후 미사용)
teams/*/knowledge/vector-index/

# === 컴파일된 Canon 은 커밋 (프레임워크 변천사 추적) ===
# (compiled.md 는 기본 커밋)

# === 에디터/OS ===
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# === Cygwin/MSYS bash 크래시 덤프 (Windows) ===
*.stackdump

# === Claude Code 로컬 설정 (사용자별 권한 등) ===
.claude/settings.local.json
.claude/worktrees/

# === 로그 ===
*.log
logs/

# === 생성된 문서 (자동 생성물) ===
docs/traceability.md

# knowledge/reference: 사용자 자료 (큰 PDF 포함, GitHub 100MB 제한). repo 외부 보관.
knowledge/reference/

# === 디렉토리 백업 zip (R&D chat ↔ Claude Code 전송용 임시, 커밋 X) ===
*.zip

# === 스크래치 산출물 (_ prefix 컨벤션. 진단·백테스트 패널 등 재생성 가능) ===
/_*.json
data/_*.csv
data/_*.json

# === Pencil MCP 내보내기 (시안 PNG. 정본은 .pen) ===
webapp/.pen-exports/

# === webapp 런타임 산출물 (추천 백업 jsonl 등) ===
webapp/data/
