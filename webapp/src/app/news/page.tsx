import { Placeholder } from "@/components/Placeholder";

export default function NewsPage() {
  return (
    <Placeholder
      title="뉴스"
      note="장기 흐름 ↔ 단기 테마 분류 뉴스 큐레이션(NEWS-SOURCE-001). 수집·분류·digest 백엔드는 동작 중 — 화면 본체는 다음 세션."
    />
  );
}
