import { Placeholder } from "@/components/Placeholder";

export default function DeskPage() {
  return (
    <Placeholder
      title="가상매매 데스크"
      note="4계좌 자산 곡선(실현 vs 총)·회고 KPI·계좌별 보유 현황. 백엔드 API(/api/accounts·/api/wealth·/api/guidance)는 동작 중 — 화면 본체는 다음 세션."
    />
  );
}
