"""자동 권고 생성 (AUTO-SIGNAL-GENERATION-001) — 두뇌↔몸통 빠진 연결.

매일 N cadence 에 watchlist 를 결정론 스크리닝→funnel(전략가 직접·우회)→권고 persist→
데스크 소비. 사용자 개입 0 으로 권고 스트림이 생성된다(방어장이면 관망 리포트).

M1 (본 모듈 watchlist) = funnel Stage 0: watchlist 합집합 + 결정론 RS·과열도 컷 (LLM 0).
"""
