---
date: 2026-05-23
topic: cycle 14.2 + 14.3 = WAVE-ALPHA collectors/anchors.py + α 3 timeframe hook + stock_analyst v5 persona + 테스트 ~75 + smoke
status: completed
plan_file: C:\Users\HOME\.claude\plans\refactored-frolicking-pie.md
---

# 2026-05-23 · WAVE-ALPHA 구현 14.2 + 14.3 (MS4 베이스라인 도달)

## 배경

cycle 14.1 (canon 21 명제 + DB v8 + scoring.alpha 시간 정규화 정식, commit `a536428`, 2026-05-22) 직후 자연 진입. RESUME Top 1 = WAVE-ALPHA 14.2 + 14.3 풀세트 한 호흡 (~1 세션). 본 세션 완료 후 → **MS4 (실 매매 시연) 베이스라인 도달**. cycle 5/9/12/14 의 "SPEC frozen → sub-cycle 구현 분할" 패턴 **6 회째 누적** (cycle 14.1 + 14.2/14.3).

## 한 일

### Step 1 — cycle 14.2 commit `7c60944` (anchors.py + run_analyst hook)

- `collectors/anchors.py` 신규 (~600 LOC)
  - `extract_swing_candidates(ohlcv, timeframe)` — Stage 1 결정론 rolling local extrema (window={daily:20, weekly:8, monthly:3}) + min_gap_bars 필터 + max_candidates 캡 (default 12)
  - `select_anchors_via_llm(ticker, timeframe, candidates, cutoff_date, skip_cache)` — Stage 2 Haiku 4.5 직관 (temperature=0.0, JSON forced) + 유효성 검증 (0 ≤ A < B < C < n) + 캐시 lookup/store
  - `_get_cached_anchor / _store_cached_anchor` — `llm_call_cache` type='anchor_selection', TTL 30 일, cache_key = "ticker|tf|cutoff"
  - `load_manual_anchors(ticker, timeframe)` — `manual_anchors` (v8 DB) SELECT 우선
  - `compute_alpha_3tf(ticker, cutoff_date)` 진입점 — 3 timeframe 풀세트 산출 (manual → Stage 1+2 → E6 fallback → unavailable)
  - `_unavailable / _build_result / _compute_one / _current_from_ohlcv` 헬퍼
  - `render_alpha_3tf_md(results, ticker)` — `[5] α 3 timeframe (WAVE-ALPHA-001)` 블록 markdown, source 명시 강제 (가드 3 정합)
  - `alpha_3tf_metadata(results)` — run_analyst metadata 노출용 (alpha_daily/weekly/monthly + source/value/label/anchors 등)

- `core/knowledge/compose.py` — `build_pipeline_prompt(..., alpha_3tf_md: str | None = None, ...)` 파라미터 신규. 블록 순서 갱신: [0] canon / [1] persona / [2] memory / [3] snapshot / [4] chart / **[5] α 3tf (신설)** / [6] fundamental / [7] RAG / [8] response rules.

- `core/inference/run_analyst.py` — `_maybe_build_alpha_3tf_md(spec, target_ticker)` helper 신규 (cycle 13 `_snapshot_extend_metadata` 패턴 mirror). `reads_chart_data=True` (stock_analyst) + target_ticker 충족 시 자동 활성. `run_analyst` + `run_analyst_stream` 양쪽에 hook 통합. metadata 에 `alpha_meta` 풀세트 노출 (alpha_3tf_failures + alpha_3tf_ticker_used + alpha_daily/weekly/monthly).

검증: pytest 468 passed (회귀 0), `from collectors.anchors import ...` import smoke OK.

### Step 2 — cycle 14.3 commit `e2ee94b` (persona v5 + 테스트 ~75 + smoke)

#### 2-A — agents/analysts/stock_analyst/persona.md v4 → v5

5 위치 정정 + § v5 정정 트레이스 신설 (v3·v4 trace 보존):

1. **§ Identity v5 헤더** — 권위 한정 4 종 확장 (α 3 timeframe + verdict/holding_period 매핑 + F1~F5 + 외삽 메타 WF4). canon WA·WF·WL·WE·WX 21 명제 ID 직접 인용 가능 영역으로 격상.
2. **§ Inputs α 3 timeframe 자동 주입** — `alpha_3tf_md [5]` 블록 출처 명시. `compute_alpha_3tf(ticker)` 진입점 / Stage 1+2 + manual override + E6 fallback 흐름 설명.
3. **§ Reasoning Doctrine α 산출 알고리즘** — 전면 재작성:
   - 시간 정규화 공식 (canon WF1·WF2·WF3): k₁ = ln(B/A)/days(A→B), k₂ = ln(current/C)/days(C→current), **α = k₂/k₁**
   - anchor 정의 (canon WA1·WA2·WA3) + 3 timeframe 독립 산출 (canon WA5)
   - THRESHOLDS 표 + TIMEFRAME_LIMITS 표
   - 5 단계 label (canon WL1) + 외삽 메타 (canon WF4) + 엣지 케이스 7 (canon WE1~WE7)
4. **§ Reasoning Doctrine verdict 매트릭스** (신설, canon WL2) — long: / swing: / 중립 분기 11 row 표 + multi-timeframe 충돌 시 "보수 우선" 룰.
5. **§ Reasoning Doctrine holding_period 매핑** (신설, canon WL3) — monthly→장기 / weekly→중기 / daily→단기, multi 시 긴 timeframe 우선. Track A/B 진입 권장 holding_period 라벨링.
6. **§ Anti-patterns 환각 가드 1중 → 3중** — 가드 1 (자료 0 시드, fractal_wave 활성으로 갱신 + 잔여 4 카테고리 자료 0 잔존) + 가드 2 (chart_data_md [4] 출처, v3 보존) + **가드 3 (anchor 출처 강제, v5 신설 핵심)** = `data.alpha_*.source` ∈ {manual, llm_stage2, deterministic_fallback, unavailable} 명시 강제. source=unavailable 시 α 추정 금지. source=deterministic_fallback 시 confidence ≤ 60.

§ "Module A 목표가 3 단" 본문 = SLOT S1 `WAVE-ALPHA-TARGETS-001` 후속 SPEC 결단 후 정식 발행으로 라벨 갱신 (본 cycle = `target_prices=null` 강제).

#### 2-B — agents/analysts/stock_analyst/manifest.yaml v4 → v5

- 헤더 v5 라벨 + 권위 한정 4 종 갱신
- `reads:` 코멘트 갱신 (fractal_wave canon 21 활성, canon_categories 에 이미 포함)
- `reads_chart_data: true` 코멘트 갱신 (chart_data [4] + α 3tf [5] 두 블록 자동 주입)
- `response_rules`:
  - 한국어 친화 용어 = timeframe 명시 강제 (`주봉 가속계수 1.6 (alpha_weekly=1.6 sweet, ...)`)
  - 인용 규칙 v5 — fractal_wave canon 21 명제 ID (`cited: [WA1, WF1, WL2, ...]`) 직접 인용
  - 환각 가드 3 중 (가드 1·2·3) 본문
  - α 산출 = WAVE-ALPHA-001 시간 정규화 정식 본문
  - verdict 매트릭스 (canon WL2) — long/swing/중립 11 row 표
  - holding_period 매핑 (canon WL3) — monthly/weekly/daily 매핑
  - Module A 목표가 3 단 SLOT S1 후속 = `target_prices=null` 강제
  - Track A read 정합 v5 확장 (alpha_daily/weekly/monthly + verdict_reasoning + holding_period + alpha_3tf_failures)

#### 2-C — collectors/anchors.py 정정 (deterministic_fallback C < current 가드)

smoke 실증 중 발견: deterministic_fallback 의 last3 candidate 가 current 와 같은 날짜 (또는 미래) 일 수 있어 `alpha()` ValueError. 정정 = `min_gap_days // 2` 이상 trailing candidate 만 채택, `usable < 3` 면 `unavailable`. canon WE6 fallback 의 본질적 가드 보강.

#### 2-D — core/db/connection.py (v8 마이그레이션 자동 적용)

기존 dev DB (v7 까지) 에서 schema.sql 의 신규 `CREATE INDEX idx_llm_call_cache_type` 가 `type` 컬럼 부재로 fail. `_ensure_schema` 가 schema.sql 실행 전 `_apply_migrations` 호출 — `_column_exists(conn, "llm_call_cache", "type")` 가드 안에서 `ALTER TABLE ... ADD COLUMN type` 멱등 실행. 14.1 에서 빠뜨린 본질 보강 (RESUME 백로그 → 본 cycle 안에서 해소).

#### 2-E — 테스트 풀세트 신규 ~74

- **tests/test_alpha.py** 신규 31 케이스 — 시간 정규화 시나리오 (수동 검증 가능 케이스 3) + interpret_alpha 5단계 timeframe 차등 (parametrize × 14) + WE2/WE3 엣지 케이스 7 + WF4 외삽 메타 5 + TIMEFRAME_LIMITS 정합 4
- **tests/test_anchors.py** 신규 38 케이스 — `extract_swing_candidates` 결정론 8 + `_anchor_cache_key` + 3 단 캐싱 (hit/miss/type 격리/TTL 만료) 7 + `select_anchors_via_llm` mock (call_llm patch, JSON parse, 유효성, 캐시 hit/skip) 8 + `load_manual_anchors` 3 + `compute_alpha_3tf` (unavailable/manual override/E6 fallback/cutoff_date/3 timeframe keys) 5 + `render_alpha_3tf_md` + `alpha_3tf_metadata` 6
- **tests/test_data_analysts_v2.py** 확장 5 — stock_analyst v5 정합 (canon WA·WF·WL·WE ID + verdict 매트릭스 키워드 + holding_period 매핑 + 환각 가드 3 + manifest response_rules WAVE-ALPHA 본문)

#### 2-F — smoke 실 호출 (005930)

`TESTING=1 uv run python -c "...compute_alpha_3tf('005930', skip_cache=True)..."`:

| timeframe | source | α | label | anchors |
|---|---|---|---|---|
| daily | deterministic_fallback | 0.44 | weak | A 2026-03-09 167,300 / B 2026-03-18 209,000 / C 2026-03-31 167,000 / cur 2026-05-22 292,500 |
| weekly | deterministic_fallback | **1.31** | **sweet** ⭐ | A 2025-11-21 94,500 / B 2026-02-27 223,000 / C 2026-04-03 167,000 / cur 2026-05-22 292,500 |
| monthly | deterministic_fallback | 3.86 | overheated | A 2024-11-30 49,900 / B 2025-07-31 74,000 / C 2025-09-30 67,500 / cur 2026-05-22 292,500 |

α 풀세트 + label 풀세트 + anchor 4 종 + progress_to_b + duration_ratio 모두 정합 산출 ✨. LLM Stage 2 (Gemini default) JSON 파싱 결함 → 모두 deterministic_fallback 으로 떨어졌으나 본 cycle 본질 (가드 강화된 fallback 정합 작동) 검증.

## 검증 결과

- ✅ pytest 전체 **542 passed** (cycle 14.1 = 468 → +74 신규 = test_alpha 31 + test_anchors 38 + test_data_analysts_v2 +5, 회귀 0)
- ✅ 005930 실 ticker smoke = α 3 timeframe 풀세트 산출 (weekly sweet 1.31 / monthly overheated 3.86)
- ✅ DB v8 자동 마이그레이션 = 기존 dev DB 호환 (column 존재 가드)
- ✅ persona v5 정정 5 위치 + v3·v4 trace 보존 (회귀 양식 통과)

## 의도적으로 안 한 것

- **LLM Stage 2 (Gemini) JSON 파싱 결함 해결** — Gemini 가 markdown JSON / single-quote / trailing comma 응답 → JSONDecodeError. SLOT S6 (`anchor LLM prompt 튜닝 + Sonnet 4.6 업그레이드 검토`) 후속. 현재는 deterministic_fallback 정합 작동으로 본 cycle 본질 (MS4 베이스라인) 충족.
- **target_prices 3 단 (보수/중립/공격)** — SLOT S1 `WAVE-ALPHA-TARGETS-001` 후속 SPEC. 본 cycle 의 persona/manifest 가 `target_prices=null` 강제.
- **SLOT S4 KIS top30 정확도 정정** (KRX devtools breadth bld) — 후속 microcycle. cycle 14.0 fallback 으로 충분.
- **production UX webapp 자연어 채팅창** (RESUME Top 2) — 다음 세션 자연 진입.

## 다음에 이어서 할 작업 (우선순위)

### 1. production UX 본질 구현 (~3 세션) — webapp 자연어 채팅창
- **왜**: MS4 베이스라인 도달 후 자연 진입. WAVE-ALPHA SPEC 부록 A 자연어 변환 사전 활용 가능. 가장 큰 사용자 가치 = 자연어 채팅창에서 의미 있는 종합 답변.
- **범위**: 자연어 intent extractor (Haiku 4.5 분류 또는 결정론 키워드 룰) + Track Selector 자동 라우팅 (이미 있음) + 종합 답변 형식 (분석가 점수 + 전략가 권고 통합 markdown) + webapp 단일 채팅창 UI 재구성 + R&D 토글 별도 페이지 보존. 30 시나리오 우주 기본 라우팅.
- **예상 산출**: 첫 자연어 호출 가능 ("삼성전자 진입할까?" → 자동 라우팅 → α + Track A·B 권고 종합 답변).

### 2. WAVE-ALPHA SLOT 후속 SPEC 묶음 (~3~4 세션 분산)
- **S1** `WAVE-ALPHA-TARGETS-001` — target_prices 3 단 (보수/중립/공격) 산출 룰. stock_analyst verdict=confirmed_high_quality 시 발행.
- **S2** `WAVE-ALPHA-WATCH-001` — 월봉 황제주 watchlist + 알림 cron (cron + telegram + watchlist DB).
- **S3** `WAVE-ALPHA-BACKTEST-001` — 백테스팅 본체 (사용자 라운드 1 본질). 본 SPEC alpha() 의 cutoff_date 친화 설계 활용.
- **S4** `WAVE-ALPHA-CANON-001` — 풀세트 canon W5+ 사용자 자가 정리 + 양질도 10 점.
- **S6** LLM Stage 2 prompt 튜닝 + Sonnet 4.6 업그레이드 검토 (운영 3 개월 후).

### 3. SLOT S4 정확도 정정 (~0.3 세션 microcycle)
- KRX 데이터시스템 manual devtools 추출 → `BLD_MARKET_BREADTH` 교체. KIS fallback 은 retention.

(추가 백로그: `NEWS-SOURCE-001` SPEC 신설 (news_curator SLOT S2 해소) / Layer 4 계좌관리자 (M5) / Layer 5 회고분석가 (M4) / `GUIDANCE-ACCURACY-TRACKER-001` 구현 / `INFRA-US-MACRO-SNAPSHOT-001` (yfinance/FRED) / `INFRA-RELIABILITY-VALIDATOR-001` (Layer 2.5/3.5 Haiku 검증) / scoring.py s_score·buy_score 정식 가중치 (SLOT S7) / streaming 토글 UI / Memory Compression SPEC / Quality Eval SPEC / MCP 패턴 차용 / KNOWLEDGE-SYNC-001 Phase 3 PROPOSAL)

## 맥락 재진입 힌트

- **sub-cycle 분할 ritual 6 회 누적** = cycle 5 / 9 / 12 / 14 (SPEC) + 14.0+14.1 (impl) + 본 cycle (14.2+14.3 impl). "SPEC frozen → 구현 분할 (1~3 사이클)" 패턴이 영구 ritual. 미래 인프라 SPEC default 진입점.
- **v8 자동 마이그레이션 패턴** = `core/db/connection.py:_apply_migrations` 가 schema.sql 실행 전 호출. 컬럼 존재 가드 안에서 ALTER 멱등 실행. 향후 v9+ 마이그레이션도 같은 패턴 (try/except 흡수 + `_column_exists` 가드).
- **deterministic_fallback 가드** = `_compute_one` 의 fallback 분기에서 `usable = [c for c in candidates if (cur_date - c[0]).days >= min_gap]` + `len(usable) < 3` 시 unavailable. canon WE6 의 본질적 안전망.
- **persona v5 / manifest v5 라벨링 규칙** = 헤더 코멘트에 v3·v4·v5 변경 누적 표기 + § "v5 정정 트레이스" 신설 + 기존 v3·v4 trace 보존 (deprecation X). 회귀 테스트 (test_stock_analyst_v3_persona / v4_persona) 가 v3/v4 흔적 검증하므로 보존 필수.
- **LLM Stage 2 (Gemini) JSON 파싱 결함** = 본 cycle 의 production smoke 가 발견. anchors.py `select_anchors_via_llm` 의 try/except 가 JSONDecodeError 흡수 → None 반환 → deterministic_fallback. SLOT S6 의 본질 = Stage 2 prompt 튜닝 + parser 강건성 + 모델 명시 (anthropic 강제 옵션 등).

## 커밋 상태

- **14.2** `7c60944` = collectors/anchors.py + run_analyst hook + compose.py alpha_3tf_md 파라미터 (3 파일, +807/-5)
- **14.3** `e2ee94b` = persona v5 + manifest v5 + anchors.py fallback 가드 + connection.py migrations 자동 + tests/test_alpha + tests/test_anchors + test_data_analysts_v2 확장 (7 파일, +1129/-87)
- **wrap-up commit** = 본 wrap-up 묶음 진행 예정 (c_worked + RESUME.md + SESSIONS.md, cycle 5/9/12/14.1 정합)
- **push 진행** (사용자 명시 요청 "커밋 푸시해" 누적)
